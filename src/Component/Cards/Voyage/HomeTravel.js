import React, { useEffect } from "react"
import { useParams } from "react-router-dom"
export const HomeTravel =({dataCards})=>{
    let {id} = useParams()
    console.log(id)
    let ResultId;
    
  console.log(dataCards)
    return (
        <div>
            
        </div>
    )
}